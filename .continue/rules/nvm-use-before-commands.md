---
description: Apply this rule whenever running terminal commands in this project
  to ensure nvm loads the correct Node version first and scripts execute in the right place.
alwaysApply: true
---

Always run `nvm use` before executing any shell/terminal command (npm, node, vite, vitest, etc.) to ensure the correct Node.js version is active.

The project directory is `/Users/katerberg/src/wizard-tower-builder`.