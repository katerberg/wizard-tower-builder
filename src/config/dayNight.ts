/** Real-time seconds per day phase. */
export const DAY_DURATION = 60;
/** Real-time seconds per night phase. */
export const NIGHT_DURATION = 90;
/** Brief flash after a side job completes before removal. */
export const SIDE_JOB_SUCCESS_FLASH_SEC = 1.5;

/** Recruit / unrecruit one staff member (at 1× sim speed). */
export const RECRUIT_SIDE_JOB_SEC = 4;
/** Apply a room modification (at 1× sim speed). */
export const MOD_SIDE_JOB_SEC = 6;
/** Enqueue a research node (at 1× sim speed). */
export const RESEARCH_ENQUEUE_SIDE_JOB_SEC = 4;
