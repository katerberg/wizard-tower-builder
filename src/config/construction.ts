/** Max stone+metal units a laborer carries per trip. */
export const LABORER_CARRY_CAPACITY = 5;
/** Build progress per second from the first laborer on site (after delivery). */
export const BUILD_PROGRESS_PER_SEC = 0.15;
/** Work units required per footprint cell. */
export const BUILD_WORK_PER_CELL = 1;
/** Fraction of physical resources returned on teardown (matches mod sell refund). */
export const TEARDOWN_REFUND_RATE = 0.5;
/** Fraction of souls returned on order cancel / teardown. */
export const SOUL_REFUND_RATE = 0.5;

export const SCAFFOLD_BLUEPRINT_ID = 'scaffold';
